import { ConsultantInfo as _ConsultantInfo } from './ConsultantInfo';
import {
  ProfilePicture,
  ConsultantName,
  ConsultantNumber,
  ConsultantTitle,
  Content,
  Icon,
} from './components';

export const ConsultantInfo = Object.assign(_ConsultantInfo, {
  Icon,
  ConsultantName,
  ConsultantNumber,
  ConsultantTitle,
  Content,
  ProfilePicture,
});
