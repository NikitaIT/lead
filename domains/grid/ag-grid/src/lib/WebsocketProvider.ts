import * as Y from 'yjs';
// import { WebsocketProvider } from 'y-websocket';

export function useWebsocketProvider(ydoc: Y.Doc) {
  // const provider = new WebsocketProvider(
  //   'wss://demos.yjs.dev',
  //   'prosemirror-versions-demo',
  //   ydoc
  // );
  // on network changed
  // provider.disconnect();
  // provider.connect();
  // for providers data integration
  // provider.awareness
}
